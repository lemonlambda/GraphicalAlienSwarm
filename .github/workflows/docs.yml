name: Docs

on:
  push:
    branches: [ "docs" ]
  pull_request:
    branches: [ "docs" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Docs
      run: cargo doc
